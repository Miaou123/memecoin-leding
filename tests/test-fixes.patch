# Test File Fixes for Memecoin Lending Protocol

## Key Changes Required:

### 1. Initialize instruction
The initialize method now requires 5 arguments instead of 3:
- admin
- buyback_wallet  
- operations_wallet
- authorized_liquidator (NEW)
- price_authority (NEW)

### 2. Method names
All methods now use snake_case:
- fundTreasury → fund_treasury
- withdrawTreasury → withdraw_treasury
- whitelistToken → whitelist_token
- updateTokenConfig → update_token_config
- createLoan → create_loan
- repayLoan → repay_loan
- pauseProtocol → pause_protocol
- resumeProtocol → resume_protocol
- updateFees → update_fees
- initializeStaking → initialize_staking
- depositRewards → deposit_rewards
- claimRewards → claim_rewards (if it exists)

### 3. Account names
When using accountsStrict(), use snake_case:
- protocolState → protocol_state
- tokenConfig → token_config
- systemProgram → system_program

### 4. initialize_staking changes
Now only takes 1 argument:
- epoch_duration (i64)

The old 4 arguments (target_balance, base_emission_rate, etc) were removed.

### 5. Missing accounts
Some instructions may need additional accounts like:
- system_program
- rent sysvar
- token_program