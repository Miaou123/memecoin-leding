# Solana Configuration
# SECURITY: RPC requests are proxied through the backend - no API key needed here
VITE_SOLANA_NETWORK="mainnet-beta"
VITE_API_URL=http://localhost:3001

# All protocol addresses and configuration are loaded from deployments/[network]-latest.json
# Do not hardcode any addresses here - use the deployment JSON as single source of truth